{{#if imageFlag}}
    <div class="Content_item_image"><img src="{{image}}"/></div>
    <div class="Content_item_category categoryColor_{{category}}">{{category_disp}}</div>
    <div class="Content_item_text">{{title}}</div>
    <div class="Content_item_star">{{{showStars rate}}}</div>
    <div class="Content_item_rank rank_{{rank}}">{{rank}}</div>
{{else}}
    <div class="Content_item_image"><img src="images/NoImage.jpg"/></div>
    <div class="Content_item_category categoryColor_{{category}}">{{category_disp}}</div>
    <div class="Content_item_text">{{title}}</div>
    <div class="Content_item_star">{{{showStars rate}}}</div>
    <div class="Content_item_rank rank_{{rank}}">{{rank}}</div>
{{/if}}